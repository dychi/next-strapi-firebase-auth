import { render, cleanup, fireEvent } from '@testing-library/react'

afterEach(cleanup)

// 要件
describe('要件の説明', () => {
  // 前準備

  // 仕様1
  describe('仕様1', () => {
    // テスト内容
    it.todo('テスト1')
  })
  // 仕様2
  describe('仕様2', () => {
    // テスト内容
    it.todo('テスト1')
  })
})
